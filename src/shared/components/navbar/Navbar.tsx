import {css} from "@emotion/react"

const header = {
    
    backgroundColor: 'red'
}

export const Navbar: React.FC = () => {
    return (
        <div style={header}>
            MJAU
        </div>
    )
}