import { Outlet} from 'react-router-dom';
import { ImagePreview } from '../../../modules';

export const Exterior: React.FC = () => {
    return (
        <div>
            <ImagePreview />
        </div>
    )
}